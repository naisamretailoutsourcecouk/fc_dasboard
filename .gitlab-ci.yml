pages:
  stage: deploy
  image: alpine:3.20
  script:
    - mkdir -p public
    - cp index.html styles.css app.js README.md public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
